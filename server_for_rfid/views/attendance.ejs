<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Attendance</title>
    <%- include('./partials/navCss') %>
    <style>
        main{
            width: 80%;
            min-height: 100vh;
            margin-left: 20%;
            padding: 2rem;
            background-color: aliceblue;
        }
        .pop-up{
            border: .2rem solid;
            background-color: aquamarine;
            font-size: 2rem;
            border-radius: .3rem;
            width: 50%;
            padding: .6rem 1rem;
        }
        .display-msgs{
            display: grid;
            margin-top: 2rem;
            padding: 1rem;
            gap: .5rem;
            grid-template-columns: auto auto auto auto;
        }
        .msg{
            border: .3rem solid aquamarine;
            border-radius: .3rem;
            padding: .5rem 1rem;
        }
        .msg{
            padding: 2rem;
            font-size: 1.5rem;
            display: grid;
            border: .1rem solid;
            margin-top: 3rem;
            text-align: center;
        }
        .msg button{
            text-decoration: none;
            border: none;
            font-size: 1.5rem;
            font-family: inherit;
            color: inherit;
            margin: 3rem auto;
            width: fit-content;
            text-align: center;
            padding: .5rem 1rem;
            background-color: aquamarine;
            border-radius: .3rem;
        }
        .atd_msg{
            text-align: center;
            background-color: aquamarine;
            width: fit-content;
            border-radius: .3rem;
            padding: .5rem 1rem;
            margin: 2rem auto;
            font-size: 1.5rem;
        }
    </style>
</head>
<body>
    <%- include('./partials/nav') %>
    <main>
        <div class="pop-up">
            Please scan ID for attendance
        </div>
        <% if (msg != '') { %>
            <div class="atd_msg">
                <%- msg %>
            </div>
            
        <% } %>
        <div class="msg">
            Press Start after Scanning card
            <form method="post" action="/attendance/record">
                <button type="submit"> Start </button>
            </form>
        </div>
        
    </main>
</body>
</html>